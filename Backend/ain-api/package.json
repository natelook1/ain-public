{
  "name": "ain-api",
  "version": "1.0.0",
  "description": "AIN standalone API service — replaces n8n webhook endpoints",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ioredis": "^5.3.2"
  }
}
